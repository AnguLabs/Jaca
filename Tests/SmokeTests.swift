import XCTest

/// Placeholder so the test target has sources. Real core tests (LogcatParser,
/// LogFilter, HistoryStore) are added alongside those components.
final class SmokeTests: XCTestCase {
    func testItRuns() {
        XCTAssertTrue(true)
    }
}
