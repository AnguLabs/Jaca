package okhttp3;

/** COMPILE-ONLY stub of okhttp3.Request (a class in real okhttp). Never bundled or
 *  instantiated — only used as a parameter/return type; the app's real Request binds at runtime. */
public class Request {
}
