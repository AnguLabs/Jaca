package okhttp3;

/** COMPILE-ONLY stub of okhttp3.Response (a class in real okhttp). Never bundled or
 *  instantiated — only used as a return type; the app's real Response is bound at runtime. */
public class Response {
}
